# ---------------------------------------------------------------------------
# Virtual environment  (~2 GB, and it is machine-specific anyway)
# ---------------------------------------------------------------------------
.venv/
venv/
env/

# ---------------------------------------------------------------------------
# Trained models
#
# autism_detector.keras is ~107 MB, which is over GitHub's 100 MB per-file
# hard limit -- a push containing it is rejected outright. Models are also
# reproducible from the code, so they are not tracked. Use Git LFS or a
# GitHub Release if you genuinely need to distribute them.
# ---------------------------------------------------------------------------
Models/*.keras
Models/*.h5
Models/*.weights.h5

# ---------------------------------------------------------------------------
# Data
#
# The ABIDE-II MRI volumes and phenotypic CSVs are ~16 GB and are distributed
# under a Data Usage Agreement that does not permit redistribution. Neither
# the raw data nor anything derived from it belongs in this repository.
# ---------------------------------------------------------------------------
ABIDEII-*/
ABIDEII-*.csv
MAIN_Training1/
MAIN_Testing1/
MAIN_Testing1_WholeMRI/
*.nii
*.nii.gz

# Run artefacts that reference local paths / specific patients
dataset_manifest.json
train_val_split.json
train_run.log
*.log

# Generated reports (regenerated by train_model.py / test_model.py)
Reports/*.png
Reports/*.txt
Reports/*.json

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# ---------------------------------------------------------------------------
# Editors / OS
# ---------------------------------------------------------------------------
.vscode/
.idea/
Thumbs.db
Desktop.ini
$RECYCLE.BIN/
~$*
